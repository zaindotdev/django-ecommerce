[project]
name = "ecommerce"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0",
    "pillow>=12.0.0",
    "python-dotenv>=1.2.1",
    "stripe>=14.1.0",
]
